const express = require('express');
const router = express.Router();

// Placeholder for library routes
router.get('/', (req, res) => {
  res.json({ message: 'Library routes - Coming soon' });
});

module.exports = router;
