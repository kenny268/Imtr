const express = require('express');
const router = express.Router();

// Placeholder for academic routes
router.get('/', (req, res) => {
  res.json({ message: 'Academic routes - Coming soon' });
});

module.exports = router;
